<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NovaktApp"
             x:Class="NovaktApp.View.LoginPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Entry Grid.Row="0" HorizontalOptions="Center" WidthRequest="200" Placeholder="Login..."/>
            <Entry Grid.Row="1" HorizontalOptions="Center" WidthRequest="200" IsPassword="True" Placeholder="Password..."/>
            <Button Grid.Row="2" HorizontalOptions="Center" WidthRequest="200" Text="Connexion" Command="{Binding ConnexionCommand}"/>
        </Grid>
    </Grid>

</ContentPage>